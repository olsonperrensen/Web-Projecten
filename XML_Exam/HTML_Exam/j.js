//$(`.first`).css(`color`, `red`);
//$(`main p`).css(`font-family`, `arial`);

// document.querySelector(`.first`).style.color = `steelblue`;
// document
//   .querySelectorAll(`main p`)
//   .forEach((p) => (p.style[`font-family`] = `arial`));

$(document).load(initjq);

function initjq() {
console.log(`html is ready`)
}